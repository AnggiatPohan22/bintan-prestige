{{-- Template: Full-width Landing — edge-to-edge, no top gap (hero-led pages). --}}
<div class="cms-page cms-page--full min-h-screen">
    @include('frontend.pages._blocks', ['page' => $page])
</div>
